  <div class="head">
        <h3 class="btn dropdown-toggle pull-right" type="button" id="menu1" data-toggle="dropdown">
          Filtrar
        </h3>
        <ul class="dropdown-menu" role="menu" aria-labelledby="menu1" style="width: 20%;">
          <%= content_tag :li, role: "presentation", class: active_class("title", @sort) do %>
            <%= link_to "Título", {sort: :title}, role:"menuitem", tabindex:"-1" %>
          <% end %>
          <%= content_tag :li, role: "presentation", class: active_class("published", @sort) do %>
            <%= link_to "Publicação", {sort: :published} , role:"menuitem", tabindex:"-1" %>
          <% end %>
          <%= content_tag :li, role: "presentation", class: active_class("created_at", @sort) do %>
            <%= link_to "Uploaded", {sort: :created_at} , role:"menuitem", tabindex:"-1" %>
          <% end %>
        </ul>
      <h1 class="page-header"><%= @books.model_name.human(count: 2) %>
        <small><%= @title %></small>
      </h1>
  </div>
</div>
<div class="panel-body">
  <% @books.in_groups_of(4, false).each do |group| %>
    <div class="row">
      <% group.each do |book| %>
        <div class="col-lg-3 col-md-4 col-sm-6 home-item">
          <div class="card h-100">
            <% if book.image.attached? %>
              <%= image_tag book.image, class:"card-img-top img-responsive" %>
            <% end %>
            <div class="card-body">
              <h4 class="card-title">
                <%= link_to user_book_path(book) do %>
                  <%= book.title %>
                <% end %>
              </h4>
            </div>
            <% if book.status == 2 %>
            <div class="card-footer">
              <%= progress_bar book.percentage, alternative: progress_alt(book.percentage), label: true, style: 'min-width: 20px' %>
            </div>
            <% end %>
          </div>
        </div>
      <% end %>
    </div>
  <% end %>
  </div>
<hr>

<!-- Pagination -->
<div class="row justify-content-center">
  <%= paginate @books %>
</div>
