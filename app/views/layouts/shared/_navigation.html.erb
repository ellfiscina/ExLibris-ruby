<nav class="navbar navbar-expand-lg navbar-light fixed-top" id="mainNav">
    <button type="button" id="sidebarCollapse" class="navbar-toggler navbar-toggler-right">
        <i class="fa fa-bars"></i>
    </button>
      <ul class="navbar-nav ml-auto">
        <li class="nav-item">
          <%= link_to "Home", root_path, class:"nav-link" %>
        </li>
        <% unless user_signed_in? %>
          <li class="nav-item">
            <%= link_to "Login", new_user_session_path, class:"nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Cadastro", new_user_registration_path, class:"nav-link" %>
          </li>
        <% else %>
          <li class="dropdown nav-item">
            <a class="dropdown-toggle nav-link" data-toggle="dropdown" href="#">
              <%= current_user.email %>
            </a>
            <ul class="dropdown-menu">
              <li>
                <%= link_to edit_user_registration_path(current_user), class:"nav-link" do%>
                <i class="fa fa-user fa-fw"></i>Perfil
                <% end %>
              </li>
              <li class="divider"></li>
              <li><%= link_to destroy_user_session_path , method: :delete, class:"nav-link" do %>
                <i class="fa fa-sign-out fa-fw"></i> Logout
                <% end %>
              </li>
            </ul>
          </li>
        <% end %>
      </ul>
</nav>