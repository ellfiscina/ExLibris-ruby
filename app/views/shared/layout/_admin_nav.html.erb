<nav class="navbar navbar-expand-lg navbar-light fixed-top" id="mainNav">
  <% if user_signed_in? %>
    <button type="button" id="sidebarCollapse" class="navbar-toggler navbar-toggler-right">
        <i class="fa fa-bars"></i>
    </button>
    <% end %>
      <ul class="navbar-nav ml-auto" style="display: inline-block;">
        <li class="nav-item">
          <%= link_to "Home", root_path, class:"nav-link" %>
        </li>
        <% unless admin_signed_in? %>
          <li class="nav-item">
            <%= link_to "Login", new_admin_session_path, class:"nav-link" %>
          </li>
        <% else %>
          <li class="dropdown nav-item">
            <a class="dropdown-toggle nav-link" data-toggle="dropdown" href="#">
              <%= current_admin.email %>
            </a>
            <ul class="dropdown-menu">
              <li>
                <%= link_to fa_icon('th', text: t('.dashboard')),
                            admin_dashboards_path,
                            class:"nav-link" %>
              </li>
              <li>
                <%= link_to fa_icon('user', text: t('.profile')),
                            edit_admin_password_path,
                            class:"nav-link" %>
              </li>
              <li class="divider"></li>
              <li><%= link_to fa_icon('sign_out', text: t('.sign_out')),
                              destroy_admin_session_path,
                              method: :delete, class:"nav-link" %>
              </li>
            </ul>
          </li>
        <% end %>
      </ul>
</nav>
